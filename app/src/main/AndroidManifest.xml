<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="site.team2dev.m_lerning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MahasiswaScoreTest"
            android:theme="@style/AppTheme.Utama">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DosenAddProdi"
            android:theme="@style/AppTheme.Utama">
        </activity>
        <activity
            android:name=".AaListProdi"
            android:theme="@style/AppTheme.Utama"></activity>
        <activity
            android:name=".AdminHome"
            android:theme="@style/AppTheme.Utama" />
        <activity
            android:name=".DosenHome"
            android:theme="@style/AppTheme.Utama" />
        <activity
            android:name=".MahasiswaHome"
            android:theme="@style/AppTheme.Utama" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoStatusAndAction" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>